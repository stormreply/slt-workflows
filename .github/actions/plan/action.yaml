name: Init

inputs:
  infracost_api_key:
    required: false

runs:
  using: composite
  steps:

      - name: checkout code
        uses: stormreply/slt-workflows/.github/actions/terraform/checkout@feat/init-tfvars-from-vars
        with:
          infracost_api_key: ${{ inputs.infracost_api_key }}

      - name: terraform plan
        uses: stormreply/slt-workflows/.github/actions/terraform/plan@main
