name: Inject init.tfvars

runs:
  using: composite
  steps:

    - name: inject init.tfvars
      shell: bash
      run: |
        # checkout/inject-init-tfvars
        cat << EOF >> init.tfvars
        backend_bucket = "${{ env.BACKEND_BUCKET }}"
        oidc_principal = "${{ env.OIDC_PRINCIPAL }}"
        EOF
        echo "init.tfvars:"
        cat init.tfvars
