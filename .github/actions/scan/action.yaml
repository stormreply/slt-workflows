name: Scan code

runs:
  using: composite
  steps:

    - name: checkout code
      uses: stormreply/slt-workflows/.github/actions/terraform/checkout@main

    - name: run pre-commit
      uses: stormreply/slt-workflows/.github/actions/scan/pre-commit@main
      id: pre-commit
