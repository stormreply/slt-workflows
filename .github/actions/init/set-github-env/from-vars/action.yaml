name: Set environment from vars

inputs:
  github_vars:
    required: true

runs:
  using: composite
  steps:

    - name: set-github-env-from-vars
      id: set-github-env-from-vars
      shell: bash
      run: |
        # set-github-env/from-vars
        echo '${{ inputs.github_vars }}' \
        | jq -r 'to_entries | map("\(.key)=\(.value)") | .[]' \
        | tee -a $GITHUB_ENV
